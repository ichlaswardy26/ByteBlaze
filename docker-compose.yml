services:
  byteblaze:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: byteblaze
    restart: unless-stopped
    user: "1003:1003"
    volumes:
      - ./app.yml:/app/app.yml        # Mount langsung ke root
      - ./logs:/app/logs              # Direktori logs
      - ./data:/app/data              # Direktori data
      - ./languages:/app/languages    # Mount langsung ke localization bahasa
    environment:
      - NODE_ENV=production
    deploy:
      resources:
        limits:
          memory: 768M                # Lebih aman untuk VPS 2GB
    networks:
      - byteblaze-net

networks:
  byteblaze-net:
    name: byteblaze-network
